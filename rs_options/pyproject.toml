[project]
name = "rs-options-risk"
version = "0.1.0"
description = "Foundational Risk Engine for the RS Options Research & Execution Engine spec v2.1"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
test = ["pytest>=7"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["rs_options_risk"]
